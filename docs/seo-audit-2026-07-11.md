# SEO audit — wsdcacademy.com (2026-07-11)

Run with the claude-seo v2.2.0 playbook: four parallel specialists (technical+sitemap,
schema, content/E-E-A-T+GEO, performance+images) against the local production build
(49 pages, post-humanize, post-store). Live domain currently serves an unrelated
"Coming Soon" placeholder. Lab-only performance (no field data pre-launch).

## SEO Health Score: 80/100

| Category | Score | Weight |
|---|---|---|
| Technical SEO | 86 | 22% |
| Content Quality | 71 | 23% |
| On-Page | 82 | 20% |
| Schema | 88 | 10% |
| Performance (lab) | 84 | 10% |
| AI Search Readiness | 78 | 10% |
| Images | 74 | 5% |

## Critical

1. **Live domain serves an indexable "Coming Soon" placeholder.** No robots.txt, no
   sitemap, no noindex → Google can index "Coming Soon" as the brand homepage. Fix:
   deploy the real build (or noindex the placeholder immediately).
2. ~~**Checkout with no legal layer.**~~ **RESOLVED 2026-07-11**: /privacy, /terms,
   /refund, /about built (53 pages now), linked in footer bottom bar + checkout consent
   line. /about substantiates the "500+ students" claim (partially resolves H4) and adds
   the entity story (resolves the About-page High). Remaining owner items: refund-policy
   sign-off + real governing-law jurisdiction in /terms.

## High

- **robots.txt disallows `/_next/`** → blocks Googlebot from CSS/JS render assets
  (mobile-first rendering). Remove `/_next/` from `src/app/robots.ts`.
- **Anonymous content**: every Article is authored by the Organization; no bylines,
  no visible dates on posts (dates only on /blog index). Coach credentials already
  exist on /coaches with Person schema. Fix: byline block in BlogPostShell (linked to
  /coaches) + Article author → Person @id + visible dateModified on pillars.
  Cheapest large E-E-A-T win.
- **Program pages (money pages) are the thinnest on the site**: ~210–250 net words vs
  800-word service floor, no named coaches, no program FAQ, no post-payment/refund
  info beside a $749 button. Expand with substance (coaches who teach it, sample
  week, program FAQ incl. withdrawal, outcomes when real).
- ~~**Unsubstantiated claims**~~ **RESOLVED 2026-07-12**: "500+" substantiated on
  /about (2026-07-11); homepage competitor absolute softened to "Most serious World
  Schools programs in America run as summer intensives."
- ~~**Cannibalization**~~ **RESOLVED 2026-07-12**: post re-angled to "USA Debate: the
  year before you apply" (metaTitle "How to Prepare for the USA Debate Application:
  The Year-Before Plan", slug unchanged); duplicated process/eligibility FAQs removed
  in favor of pillar links; new season-by-season prep section added; inbound anchors
  fixed sitewide (team/logistics anchors → pillar, prep anchors → post); pillar now
  links the post as "the year-before training plan".
- **No /about page** — the entity story exists only in fragments. Needed for QRG
  Who/How/Why, claim substantiation, and AI entity resolution.
- **Hero LCP image 295KB, AVIF disabled** — `next.config.ts` is empty so images are
  WebP-only; hero source (tournament-team-harvard.jpg, 693KB) is over-encoded. Add
  `images: { formats: ['image/avif', 'image/webp'] }` + re-export the photo.
- **/coaches above-fold headshots are lazy-loaded** (LCP element itself is lazy).
  Add `priority` to the first row.
- **Geist Mono (23KB) preloaded sitewide, used only in checkout step indicators.**
  Scope to checkout layout or use the ui-monospace fallback stack.
- **Title lengths**: 34/40 pages exceed 60 chars (61–91). ⚠ OWNER DIRECTIVE: long
  keyword-led titles are deliberate here — do not trim without owner sign-off. Google
  truncates display and may rewrite; owner accepts this trade-off. (Same for 30
  descriptions over 160 chars.)

## Medium

- Sitemap `lastmod` = build timestamp for 23 non-blog URLs (sitemap.ts uses `new
  Date()`) → per-page real dates or omit.
- No security headers for Netlify deploy (HSTS, nosniff, X-Frame-Options,
  Referrer-Policy) + `poweredByHeader: false`. Add netlify.toml or next.config headers.
- Default Next 404 (status/noindex correct) → add branded not-found.tsx with links.
- Tournament-post overlap: US-circuit post embeds a full internationals table
  duplicating the international post. Compress to top-3 + link; retitle US post
  toward "US Circuit Guide".
- "world schools debate format" mild split between /what-is pillar and
  /resources/wsdc-format-quick-reference → retitle resource to lead with "WSDC".
- Zero off-site entity footprint: no Organization/Person `sameAs`, no social
  profiles, no GBP. Create LinkedIn/YouTube, then wire sameAs (blocked on profiles
  existing).
- Article schema missing `image` (all 6 Article pages) → pass OG image per page.
- Course schema likely ineligible for Google Course-info: move Offer (with
  category:"Paid") to Course level + add courseWorkload/complete Schedule.
- Bloated source images: 5 files >200KB in public/images (worst: zach-fleeser.jpg
  488KB at 600×529). Re-encode <150KB each. Headshots below retina res; one
  (shaurya, 200×200) upscaled at 1x. Hero source 1600px vs ~2400px ideal.
- Blog posts mostly ~650–950 net words vs 1,500 blog floor. Do NOT pad (owner rule);
  where genuinely useful add one worked example/drill per head-term post
  (rebuttal-guide, POI). Coaches page bios are bullets only (~270 net words).
- /trial page thin (~90 net words) for the primary conversion action.

## Low / Info

- FAQPage schema: Google retired FAQ rich results May 7 2026 — keep (AI citation
  value), don't add more expecting SERP benefit. Home FAQ is a subset of /faq (moot).
- Blog Article author→named Person, coach Person `sameAs`, Course provider @id ref,
  breadcrumb Home trailing slash — schema polish items.
- Checkout pages: noindex but canonical→homepage (conflicting directives, harmless);
  give the segment its own metadata. `/checkout/confirmation` is crawl-blocked so its
  noindex can't be read — acceptable.
- IndexNow not implemented (Bing/Yandex speed-up, backlog). llms.txt absent (minor).
- Sitemap priority/changefreq ignored by Google — can slim.
- All 14 posts dated Jul 9–10 2026 (bulk publish) — stagger future "Updated" dates.
- JS baseline 168KB gz/page = Next framework floor; fine. 0 third-party requests —
  keep it that way (Stripe.js already checkout-only). CLS measured 0 on all pages.

## Verified strong (don't re-litigate)

- 100% of 38 sitemap URLs return 200; zero broken internal links; zero orphans;
  unique titles/descriptions; exact self-canonicals; single H1s; clean hierarchy;
  full SSR (nothing JS-gated); clean URL structure with single 308s.
- 52/52 JSON-LD blocks parse; zero deprecated types; zero aggregateRating; all URLs
  production-absolute; schema text matches visible text (16/16 spot checks).
- GEO: answer-first definition blocks, extractable tables, 134–167-word FAQ answers,
  citation-worthy unique data (33-tournament TOC bid tiers, verified international
  directory with dated methodology), 25+ primary-source external links on the
  tournaments post, all AI crawlers allowed.
- Performance: 7.4KB CSS, zero third-party, all JS async, fonts preloaded with
  swap + size-adjust fallbacks, measured CLS 0.00 everywhere, 68–96ms lab LCP on
  text-LCP pages. All images through next/image with correct srcset/sizes and 100%
  descriptive alt coverage.

## Sequenced plan

**Before deploy (same session as deploy):** legal pages + /about → robots /_next/ fix
→ AVIF config + recompress 5 images + un-lazy coaches row + scope Geist Mono →
security headers (netlify.toml) → not-found.tsx.
**Deploy cutover:** replace placeholder, verify apex/www redirect + HTTPS, submit
sitemap in GSC, verify deploy previews are noindexed.
**Week 1 after:** bylines + visible dates + Article image/author schema → program
page expansion → claim substantiation.
**Month 1:** USA Debate dedupe, tournament-post overlap trim, Course schema
restructure, social profiles + sameAs, IndexNow, coach bio prose, /trial expansion.

Falsifiability: GSC coverage (placeholder gone, 38 URLs indexed), Search appearance
for "wsdc prep" brand query, PageSpeed Insights LCP <2.5s on / after image fixes,
Course/Article enhancements visible in GSC enhancement reports.
