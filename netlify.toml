# Netlify build config for the Next.js 16 App Router site.
# The official plugin handles SSR pages, API routes, redirects, and image optimization.
[build]
  command = "next build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
